import { VivaPaymentResultClient } from "../../../components/VivaPaymentResultClient";
import { requireVivaPayments } from "../../../lib/viva-runtime";

type Props={searchParams:Promise<Record<string,string|string[]|undefined>>};
function one(v:string|string[]|undefined){return Array.isArray(v)?v[0]:v}
export default async function Page({searchParams}:Props){const q=await searchParams,t=one(q.t),s=one(q.s);let confirmed=false,orderId:string|undefined,message="Η πληρωμή ελέγχεται με τη Viva.";try{if(!t||!s)throw new Error("Missing Viva return identifiers");const result=await requireVivaPayments().reconcileTransaction({transactionId:t,expectedOrderCode:s,source:"redirect",now:Date.now()});confirmed=result.paymentStatus==="captured";orderId=result.orderId;message=confirmed?"Η πληρωμή επιβεβαιώθηκε και η παραγγελία σου καταχωρήθηκε.":"Η Viva δεν έχει ακόμη επιβεβαιώσει οριστικά την πληρωμή. Η παραγγελία παραμένει σε αναμονή."}catch{message="Δεν μπορέσαμε να επιβεβαιώσουμε την πληρωμή από τη σελίδα επιστροφής. Δεν θα θεωρήσουμε την παραγγελία πληρωμένη μέχρι να επιβεβαιωθεί από Viva/webhook."}return <main className="shell section"><VivaPaymentResultClient confirmed={confirmed}/><div className="eyebrow">Viva Smart Checkout</div><h1>{confirmed?"Η πληρωμή ολοκληρώθηκε":"Η πληρωμή ελέγχεται"}</h1><p className="lead compact">{message}</p>{orderId&&<p><strong>Παραγγελία:</strong> {orderId}</p>}<div className="hero-actions"><a className="button" href="/account">Ο λογαριασμός μου</a><a className="button button-secondary" href="/shop">Συνέχεια στις αγορές</a></div></main>}
