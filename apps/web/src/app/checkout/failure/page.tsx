import { requireVivaPayments } from "../../../lib/viva-runtime";
type Props={searchParams:Promise<Record<string,string|string[]|undefined>>};function one(v:string|string[]|undefined){return Array.isArray(v)?v[0]:v}
export default async function Page({searchParams}:Props){const q=await searchParams,t=one(q.t),s=one(q.s);let detail="Η πληρωμή δεν ολοκληρώθηκε. Το καλάθι σου παραμένει διαθέσιμο για νέα προσπάθεια.";if(t&&s){try{const r=await requireVivaPayments().reconcileTransaction({transactionId:t,expectedOrderCode:s,source:"redirect",now:Date.now()});if(r.paymentStatus==="captured")detail="Η Viva επιβεβαίωσε τελικά την πληρωμή. Δες την παραγγελία σου στον λογαριασμό σου."}catch{/* Failure redirects are advisory; webhook/API verification remains authoritative. */}}return <main className="shell section"><div className="eyebrow">Viva Smart Checkout</div><h1>Η πληρωμή δεν ολοκληρώθηκε</h1><p className="lead compact">{detail}</p><div className="hero-actions"><a className="button" href="/checkout">Δοκίμασε ξανά</a><a className="button button-secondary" href="/cart">Πίσω στο καλάθι</a></div></main>}
