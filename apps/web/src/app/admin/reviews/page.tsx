import{redirect}from"next/navigation";import{AdminWorkspaceHeader}from"../../../components/AdminWorkspaceHeader";import{AdminActionButton}from"../../../components/AdminActionButton";import{adminReviewsWorkspace}from"../../../lib/admin-governance-runtime";import{getAdminSession}from"../../../lib/admin-session";export default async function Page(){const p=await getAdminSession();if(!p)redirect('/admin/login');let d;try{d=await adminReviewsWorkspace(p)}catch{redirect('/admin')}return <main className="vendor-app admin-app"><AdminWorkspaceHeader csrfToken={d.csrfToken}/><section className="shell vendor-hero vendor-hero-compact"><div><div className="eyebrow">Verified interaction moderation</div><h1>Reviews</h1></div></section><section className="shell vendor-section"><div className="vendor-order-list">{d.reviews.length?d.reviews.map(r=><article className="vendor-order" key={r.id}><div className="vendor-order-head"><div><strong>{r.rating}/5 · {r.interactionType}</strong><small>{r.vendorId} · {r.canonicalVariantId}</small></div><span className="status-pill">{r.status}</span></div><p>{r.body??'No written comment'}</p><div className="admin-button-row"><AdminActionButton label="Publish" endpoint="/api/admin/reviews/action" csrfToken={d.csrfToken} body={{reviewId:r.id,status:'published'}} reasonPrompt="Moderation reason"/><AdminActionButton label="Hide" endpoint="/api/admin/reviews/action" csrfToken={d.csrfToken} body={{reviewId:r.id,status:'hidden'}} reasonPrompt="Moderation reason"/><AdminActionButton label="Reject" endpoint="/api/admin/reviews/action" csrfToken={d.csrfToken} body={{reviewId:r.id,status:'rejected'}} reasonPrompt="Moderation reason" danger/></div></article>):<p className="account-gate">No production-web reviews have been submitted yet. Moderation is ready for verified-review data.</p>}</div><h2 className="admin-subheading">Vendor reports</h2><div className="vendor-order-list">{d.reports.map(r=><article className="vendor-order" key={r.id}><strong>{r.reason}</strong><small>{r.details}</small><div className="admin-button-row"><AdminActionButton label="Under review" endpoint="/api/admin/reviews/report" csrfToken={d.csrfToken} body={{reportId:r.id,status:'under_review'}}/><AdminActionButton label="Resolve" endpoint="/api/admin/reviews/report" csrfToken={d.csrfToken} body={{reportId:r.id,status:'resolved'}} reasonPrompt="Resolution"/><AdminActionButton label="Reject report" endpoint="/api/admin/reviews/report" csrfToken={d.csrfToken} body={{reportId:r.id,status:'rejected'}} reasonPrompt="Resolution"/></div></article>)}</div></section></main>}
