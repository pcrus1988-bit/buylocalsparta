import{redirect}from"next/navigation";import{AdminWorkspaceHeader}from"../../../components/AdminWorkspaceHeader";import{AdminJsonForm}from"../../../components/AdminJsonForm";import{AdminActionButton}from"../../../components/AdminActionButton";import{adminContentWorkspace}from"../../../lib/admin-governance-runtime";import{getAdminSession}from"../../../lib/admin-session";export default async function Page(){const p=await getAdminSession();if(!p)redirect('/admin/login');let d;try{d=await adminContentWorkspace(p)}catch{redirect('/admin')}return <main className="vendor-app admin-app"><AdminWorkspaceHeader csrfToken={d.csrfToken}/><section className="shell vendor-hero vendor-hero-compact"><div><div className="eyebrow">Versioned Greek-first content</div><h1>CMS & SEO</h1><p className="lead">Pages are governed Core content records. The current storefront homepage remains code-rendered until the public CMS renderer is cut over.</p></div></section><section className="shell vendor-section"><AdminJsonForm endpoint="/api/admin/content" csrfToken={d.csrfToken} label="Create draft" fields={[{name:'slug',label:'Slug'},{name:'title',label:'Greek title'},{name:'description',label:'SEO / intro description'}]}/><div className="vendor-order-list admin-list-gap">{d.pages.map(pg=><article className="vendor-order" key={pg.id}><div className="vendor-order-head"><div><strong>/{pg.slug}</strong><small>v{pg.version} · {pg.pageType}</small></div><span className="status-pill">{pg.status}</span></div><div className="admin-button-row">{pg.status!=='published'&&pg.status!=='archived'&&<AdminActionButton label="Publish" endpoint="/api/admin/content/action" csrfToken={d.csrfToken} body={{pageId:pg.id,action:'publish'}}/>}{pg.status!=='archived'&&<AdminActionButton label="Archive" endpoint="/api/admin/content/action" csrfToken={d.csrfToken} body={{pageId:pg.id,action:'archive'}} reasonPrompt="Archive reason" danger/>}{pg.status==='archived'&&<AdminActionButton label="Restore draft" endpoint="/api/admin/content/action" csrfToken={d.csrfToken} body={{pageId:pg.id,action:'restore'}}/>}</div></article>)}</div></section></main>}
