import type { Metadata } from "next";import { redirect } from "next/navigation";import { AdminWorkspaceHeader } from "../../../components/AdminWorkspaceHeader";import { AdminShippingClient } from "../../../components/AdminShippingClient";import { getAdminSession } from "../../../lib/admin-session";import { adminBoxNowOrigins,boxNowShippingEnabled } from "../../../lib/boxnow-shipping-runtime";import { assertAdminPermission } from "../../../lib/admin-runtime";
export const metadata:Metadata={title:"Admin · Shipping",robots:{index:false,follow:false}};
export default async function Page(){const p=await getAdminSession();if(!p)redirect('/admin/login');assertAdminPermission(p,'fulfilment.write');if(!boxNowShippingEnabled())return <main className="vendor-app admin-app"><AdminWorkspaceHeader csrfToken={p.csrfToken}/><section className="shell vendor-hero vendor-hero-compact"><div><div className="eyebrow">Courier</div><h1>BOX NOW</h1><p className="lead">Configure credentials before mapping Vendor origins.</p></div></section></main>;return <main className="vendor-app admin-app"><AdminWorkspaceHeader csrfToken={p.csrfToken}/><section className="shell vendor-hero vendor-hero-compact"><div><div className="eyebrow">Courier origins</div><h1>BOX NOW location mapping</h1><p className="lead">Map each Vendor fulfilment location to its BOX NOW Partner API origin ID. This is platform-controlled configuration.</p></div></section><AdminShippingClient csrfToken={p.csrfToken} rows={await adminBoxNowOrigins(p)}/></main>}
