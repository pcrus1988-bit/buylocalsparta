import{redirect}from"next/navigation";import{AdminWorkspaceHeader}from"../../../components/AdminWorkspaceHeader";import{AdminJsonForm}from"../../../components/AdminJsonForm";import{adminCategoryWorkspace}from"../../../lib/admin-governance-runtime";import{getAdminSession}from"../../../lib/admin-session";export default async function Page(){const p=await getAdminSession();if(!p)redirect('/admin/login');let d;try{d=await adminCategoryWorkspace(p)}catch{redirect('/admin')}return <main className="vendor-app admin-app"><AdminWorkspaceHeader csrfToken={d.csrfToken}/><section className="shell vendor-hero vendor-hero-compact"><div><div className="eyebrow">Commerce policy governance</div><h1>Categories</h1></div></section><section className="shell vendor-section"><AdminJsonForm endpoint="/api/admin/categories" csrfToken={d.csrfToken} label="Save policy" fields={[{name:'categoryCode',label:'Category code'},{name:'labelEl',label:'Greek label'},{name:'commerceMode',label:'Commerce mode',type:'select',options:['standard','logistics_sensitive','compatibility_sensitive','regulated_mixed','vehicles','directory_only']}]}/><div className="vendor-product-grid admin-list-gap">{d.categories.map(c=><article className="vendor-product" key={c.categoryCode}><strong>{c.labelEl}</strong><small>{c.categoryCode}</small><p>{c.commerceMode} · advice {String(c.adviceAllowed!==false)} · counteroffer {String(c.counterofferAllowed!==false)}</p></article>)}</div></section></main>}
