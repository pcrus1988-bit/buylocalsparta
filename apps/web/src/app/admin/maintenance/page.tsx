import{redirect}from"next/navigation";import{AdminWorkspaceHeader}from"../../../components/AdminWorkspaceHeader";import{AdminActionButton}from"../../../components/AdminActionButton";import{adminMaintenanceWorkspace}from"../../../lib/admin-governance-runtime";import{getAdminSession}from"../../../lib/admin-session";export default async function Page(){const p=await getAdminSession();if(!p)redirect('/admin/login');let d;try{d=await adminMaintenanceWorkspace(p)}catch{redirect('/admin')}return <main className="vendor-app admin-app"><AdminWorkspaceHeader csrfToken={d.csrfToken}/><section className="shell vendor-hero vendor-hero-compact"><div><div className="eyebrow">Scheduler & search projection</div><h1>Maintenance</h1><p className="lead">Preview runtime controls exercise the same lease/reconcile contracts. Production requires durable worker processes and PostgreSQL-backed job state.</p></div></section><section className="shell vendor-section"><AdminActionButton label="Run due maintenance" endpoint="/api/admin/maintenance/run" csrfToken={d.csrfToken}/><h2 className="admin-subheading">Jobs</h2><div className="vendor-product-grid">{d.jobNames.map(j=><article className="vendor-product" key={j.name}><strong>{j.name}</strong><p>{j.state?`next ${new Date(j.state.nextRunAt).toLocaleString('el-GR')} · failures ${j.state.consecutiveFailures}`:'Due / not yet leased'}</p></article>)}</div><h2 className="admin-subheading">Search projection</h2><p>{d.indexedDocuments.length} canonical product documents currently indexed.</p></section></main>}
