import{redirect}from"next/navigation";import{AdminWorkspaceHeader}from"../../../components/AdminWorkspaceHeader";import{AdminActionButton}from"../../../components/AdminActionButton";import{adminPrivacyWorkspace}from"../../../lib/admin-governance-runtime";import{getAdminSession}from"../../../lib/admin-session";export default async function Page(){const p=await getAdminSession();if(!p)redirect('/admin/login');let d;try{d=await adminPrivacyWorkspace(p)}catch{redirect('/admin')}return <main className="vendor-app admin-app"><AdminWorkspaceHeader csrfToken={d.csrfToken}/><section className="shell vendor-hero vendor-hero-compact"><div><div className="eyebrow">GDPR operations</div><h1>Privacy requests</h1><p className="lead">{d.overdue} overdue requests. Completion never implies statutory records were erased when retention remains required.</p></div></section><section className="shell vendor-section"><div className="vendor-order-list">{d.requests.map(r=><article className="vendor-order" key={r.id}><div className="vendor-order-head"><div><strong>{r.type}</strong><small>{r.userId} · target {new Date(r.targetAt).toLocaleDateString('el-GR')}</small></div><span className="status-pill">{r.status}</span></div><div className="admin-button-row">{r.status==='submitted'&&<AdminActionButton label="Start processing" endpoint="/api/admin/privacy/action" csrfToken={d.csrfToken} body={{requestId:r.id,action:'start'}}/>}{['submitted','processing'].includes(r.status)&&<><AdminActionButton label="Complete" endpoint="/api/admin/privacy/action" csrfToken={d.csrfToken} body={{requestId:r.id,action:'complete'}}/><AdminActionButton label="Partial + retention" endpoint="/api/admin/privacy/action" csrfToken={d.csrfToken} body={{requestId:r.id,action:'partial'}}/></>}</div></article>)}</div></section></main>}
