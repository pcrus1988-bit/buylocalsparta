import { deliveryDriverWorkspace, driverClaimDeliveryJob, driverScanDeliveryProof, driverSetLiveTracking } from "../../../../lib/delivery-driver-runtime";
import { requireDeliveryDriverSession } from "../../../../lib/delivery-driver-session";
export async function GET(){try{const principal=await requireDeliveryDriverSession();return Response.json(await deliveryDriverWorkspace(principal),{headers:{"Cache-Control":"no-store"}});}catch(error){return Response.json({error:error instanceof Error?error.message:"driver_auth_required"},{status:401});}}
export async function POST(request:Request){try{const principal=await requireDeliveryDriverSession(request,true),body=await request.json() as Record<string,unknown>,action=String(body.action??"");if(action==="claim")return Response.json(await driverClaimDeliveryJob(principal,String(body.jobId??"")));if(action==="scan")return Response.json(await driverScanDeliveryProof(principal,String(body.token??"")));if(action==="tracking")return Response.json(await driverSetLiveTracking(principal,{jobId:String(body.jobId??""),enabled:body.enabled===true}));return Response.json({error:"Unknown action"},{status:400});}catch(error){return Response.json({error:error instanceof Error?error.message:"delivery_operation_failed"},{status:400});}}
