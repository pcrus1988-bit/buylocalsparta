import { getProductionPostgresRuntime } from "../../../../lib/postgres-runtime";
export const runtime="nodejs";export const dynamic="force-dynamic";
export async function GET(request:Request){const url=new URL(request.url);const q=url.searchParams.get("q")?.trim()||"";if(!q)return Response.json({suggestions:[]});try{if(process.env.BLS_SEARCH_ENABLED!=="true")return Response.json({suggestions:[]});const search=getProductionPostgresRuntime().search;if(!search)throw new Error("Search runtime unavailable");const suggestions=await search.autocomplete({marketId:"sparta",q,limit:8});return Response.json({suggestions},{headers:{"Cache-Control":"private, max-age=10"}});}catch(error){return Response.json({error:error instanceof Error?error.message:"Search unavailable"},{status:503});}}
