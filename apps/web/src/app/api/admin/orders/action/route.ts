import{requireAdminSession}from"../../../../../lib/admin-session";import{adminCancelOrder,adminOrdersReturnsWorkspace}from"../../../../../lib/admin-governance-runtime";export async function POST(r:Request){try{const p=await requireAdminSession(r,{csrf:true});const b=await r.json() as Record<string,unknown>;if(b.action!=="cancel")throw new Error("Unsupported order action");await adminCancelOrder(p,{orderId:String(b.orderId??""),reason:String(b.reason??"")});return Response.json(await adminOrdersReturnsWorkspace(p))}catch(e){return Response.json({error:e instanceof Error?e.message:"order_action_failed"},{status:400})}}
