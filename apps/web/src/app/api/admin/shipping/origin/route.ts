import { requireAdminSession } from "../../../../../lib/admin-session";import { configureAdminBoxNowOrigin } from "../../../../../lib/boxnow-shipping-runtime";
export async function POST(request:Request){try{const p=await requireAdminSession(request,{csrf:true,permission:'fulfilment.write'});const b=await request.json() as {vendorLocationId?:unknown;providerLocationId?:unknown};const vendorLocationId=typeof b.vendorLocationId==='string'?b.vendorLocationId.trim():'';const providerLocationId=typeof b.providerLocationId==='string'?b.providerLocationId.trim():'';if(!vendorLocationId||!providerLocationId)throw new Error('Both location identifiers are required');await configureAdminBoxNowOrigin(p,{vendorLocationId,providerLocationId});return Response.json({ok:true})}catch(e){return Response.json({error:e instanceof Error?e.message:'boxnow_origin_failed'},{status:400})}}
