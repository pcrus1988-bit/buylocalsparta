import { requireAdminSession } from "../../../../../lib/admin-session";import { adminApprovePayable, adminFinanceWorkspace } from "../../../../../lib/admin-runtime";
export async function POST(request:Request){try{const p=await requireAdminSession(request,{csrf:true,permission:"finance.write"});const b=await request.json() as {procurementId?:unknown};await adminApprovePayable(p,typeof b.procurementId==="string"?b.procurementId:"");return Response.json(await adminFinanceWorkspace(p))}catch(e){return Response.json({error:e instanceof Error?e.message:"procurement_action_failed"},{status:400})}}
