import{requireAdminSession}from"../../../../lib/admin-session";import{adminCategoryWorkspace,adminUpsertCategory}from"../../../../lib/admin-governance-runtime";const modes=new Set(["standard","logistics_sensitive","compatibility_sensitive","regulated_mixed","vehicles","directory_only"]);export async function POST(r:Request){try{const p=await requireAdminSession(r,{csrf:true,permission:"catalog.write"});const b=await r.json() as Record<string,unknown>;const mode=String(b.commerceMode??"");if(!modes.has(mode))throw new Error("Invalid commerce mode");await adminUpsertCategory(p,{categoryCode:String(b.categoryCode??""),labelEl:String(b.labelEl??""),commerceMode:mode as any});return Response.json(await adminCategoryWorkspace(p))}catch(e){return Response.json({error:e instanceof Error?e.message:"category_action_failed"},{status:400})}}
