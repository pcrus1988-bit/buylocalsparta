import { adminAssignDeliveryJob, adminCreateDeliveryDriver, adminResetDeliveryDriverPassword, adminSetDeliveryDriverStatus, deliveryAdminWorkspace, synchronizeDeliveryJobs } from "../../../../lib/delivery-driver-runtime";
import { requireAdminSession } from "../../../../lib/admin-session";
export async function GET(request:Request){try{const principal=await requireAdminSession(request,{permission:"fulfilment.read"});return Response.json(await deliveryAdminWorkspace(principal),{headers:{"Cache-Control":"no-store"}});}catch(error){return Response.json({error:error instanceof Error?error.message:"admin_auth_required"},{status:401});}}
export async function POST(request:Request){try{const principal=await requireAdminSession(request,{csrf:true,permission:"fulfilment.write"}),body=await request.json() as Record<string,unknown>,action=String(body.action??"");if(action==="create_driver")return Response.json(await adminCreateDeliveryDriver(principal,{partnerName:String(body.partnerName??""),displayName:String(body.displayName??""),email:String(body.email??""),phone:String(body.phone??""),password:String(body.password??"")}));if(action==="set_driver_status")return Response.json(await adminSetDeliveryDriverStatus(principal,{driverId:String(body.driverId??""),status:String(body.status??"") as "active"|"inactive"|"suspended"}));if(action==="reset_password")return Response.json(await adminResetDeliveryDriverPassword(principal,{driverId:String(body.driverId??""),password:String(body.password??"")}));if(action==="assign_job")return Response.json(await adminAssignDeliveryJob(principal,{jobId:String(body.jobId??""),driverId:String(body.driverId??"")}));if(action==="sync"){await synchronizeDeliveryJobs();return Response.json({ok:true});}return Response.json({error:"Unknown action"},{status:400});}catch(error){return Response.json({error:error instanceof Error?error.message:"delivery_admin_operation_failed"},{status:400});}}
