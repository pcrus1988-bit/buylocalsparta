import{requireAdminSession}from"../../../../../lib/admin-session";import{adminRecallWorkspace,adminResolveRecall}from"../../../../../lib/admin-governance-runtime";export async function POST(r:Request){try{const p=await requireAdminSession(r,{csrf:true,permission:"returns.manage"});const b=await r.json() as Record<string,unknown>;await adminResolveRecall(p,{noticeId:String(b.noticeId??""),resolution:String(b.reason??b.resolution??""),restoreProduct:b.restoreProduct===true});return Response.json(await adminRecallWorkspace(p))}catch(e){return Response.json({error:e instanceof Error?e.message:"recall_action_failed"},{status:400})}}
