import { adminTransmitMyData } from "../../../../../lib/admin-runtime";import { requireAdminSession } from "../../../../../lib/admin-session";
export async function POST(request:Request){try{const p=await requireAdminSession(request,{csrf:true});const b=await request.json() as {documentId?:unknown};if(typeof b.documentId!=="string"||!b.documentId.trim())throw new Error("documentId is required");const result=await adminTransmitMyData(p,b.documentId.trim());return Response.json(result);}catch(e){return Response.json({error:e instanceof Error?e.message:"mydata_transmission_failed"},{status:400})}}
