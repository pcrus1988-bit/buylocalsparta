import{requireAdminSession}from"../../../../lib/admin-session";import{adminContentWorkspace,adminCreateContentPage}from"../../../../lib/admin-governance-runtime";export async function POST(r:Request){try{const p=await requireAdminSession(r,{csrf:true,permission:"content.write"});const b=await r.json() as Record<string,unknown>;await adminCreateContentPage(p,{slug:String(b.slug??""),title:String(b.title??""),description:String(b.description??"")});return Response.json(await adminContentWorkspace(p))}catch(e){return Response.json({error:e instanceof Error?e.message:"content_create_failed"},{status:400})}}
