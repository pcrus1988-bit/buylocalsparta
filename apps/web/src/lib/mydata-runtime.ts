import { myDataConfigured, myDataConfigFromEnv, myDataIssuanceEnabled } from "@buy-local-sparta/aade-mydata";
import { getProductionPostgresRuntime } from "./postgres-runtime";
export function myDataReadiness(){const enabled=myDataIssuanceEnabled(process.env);if(!enabled)return{enabled:false,ready:true,message:"AADE myDATA automatic issuance is gated pending approved accounting mapping"};try{if(!myDataConfigured(process.env))return{enabled:true,ready:false,message:"AADE myDATA credentials are missing"};const config=myDataConfigFromEnv(process.env);if(!process.env.BLS_MYDATA_MAPPING_VERSION?.trim())return{enabled:true,ready:false,message:"BLS_MYDATA_MAPPING_VERSION is required"};if(!process.env.DATABASE_URL?.trim())return{enabled:true,ready:false,message:"AADE myDATA issuance requires PostgreSQL"};if(!getProductionPostgresRuntime().myData)return{enabled:true,ready:false,message:"AADE myDATA runtime is not initialized"};return{enabled:true,ready:true,environment:config.environment,specVersion:config.specVersion,message:"AADE myDATA ERP transport is configured; accounting mapping remains deployment-controlled"};}catch(error){return{enabled:true,ready:false,message:error instanceof Error?error.message:String(error)}}}
