name: Catalogue Identity Acceptance

on:
  pull_request:
    paths:
      - "db/migrations/0132_unified_catalog_identity.sql"
      - "db/migrations/checksums.0132.json"
      - "db/migrations/0133_vendor_product_identity_v2.sql"
      - "db/migrations/checksums.0133.json"
      - "packages/postgres-runtime/src/index.ts"
      - "scripts/catalog-identity-unification-smoke.sql"
      - "scripts/vendor-product-identity-v2-smoke.sql"
      - "apps/web/src/lib/vendor-canonical-prefill-service.ts"
      - ".github/workflows/catalog-identity-unification-acceptance.yml"

jobs:
  identity:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgis/postgis:18-3.6
        env:
          POSTGRES_USER: buy_local_sparta
          POSTGRES_PASSWORD: ci-password
          POSTGRES_DB: buy_local_sparta
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U buy_local_sparta -d buy_local_sparta"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 10
    env:
      DATABASE_URL: postgresql://buy_local_sparta:ci-password@127.0.0.1:5432/buy_local_sparta
      BLS_AUTH_SECRET: ci-only-auth-secret-0123456789-abcdefghijklmnopqrstuvwxyz
      BLS_ENABLE_DEMO_ACCOUNTS: "false"
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
      - name: Install locked workspace
        run: npm ci --ignore-scripts
      - name: Verify immutable migration digest and ledger
        run: |
          sha256sum db/migrations/0132_unified_catalog_identity.sql
          sha256sum db/migrations/0133_vendor_product_identity_v2.sql
          npm run db:verify
      - name: Bootstrap Supabase-compatible roles and Vault contract
        run: |
          psql "$DATABASE_URL" -v ON_ERROR_STOP=1 <<'SQL'
          DO $$
          BEGIN
            IF NOT EXISTS (SELECT 1 FROM pg_roles WHERE rolname = 'anon') THEN CREATE ROLE anon NOLOGIN NOSUPERUSER NOCREATEDB NOCREATEROLE NOREPLICATION NOBYPASSRLS; END IF;
            IF NOT EXISTS (SELECT 1 FROM pg_roles WHERE rolname = 'authenticated') THEN CREATE ROLE authenticated NOLOGIN NOSUPERUSER NOCREATEDB NOCREATEROLE NOREPLICATION NOBYPASSRLS; END IF;
            IF NOT EXISTS (SELECT 1 FROM pg_roles WHERE rolname = 'service_role') THEN CREATE ROLE service_role NOLOGIN NOSUPERUSER NOCREATEDB NOCREATEROLE NOREPLICATION NOBYPASSRLS; END IF;
          END
          $$;
          CREATE SCHEMA IF NOT EXISTS vault;
          CREATE TABLE IF NOT EXISTS vault.secrets (
            id uuid PRIMARY KEY DEFAULT gen_random_uuid(), name text UNIQUE, description text,
            secret text NOT NULL, key_id uuid, nonce bytea,
            created_at timestamptz NOT NULL DEFAULT now(), updated_at timestamptz NOT NULL DEFAULT now()
          );
          CREATE OR REPLACE VIEW vault.decrypted_secrets AS
            SELECT id,name,description,secret,secret AS decrypted_secret,key_id,nonce,created_at,updated_at FROM vault.secrets;
          CREATE OR REPLACE FUNCTION vault.create_secret(new_secret text,new_name text,new_description text,new_key_id uuid)
          RETURNS uuid LANGUAGE plpgsql AS $$
          DECLARE created_id uuid := gen_random_uuid();
          BEGIN
            INSERT INTO vault.secrets(id,secret,name,description,key_id) VALUES(created_id,new_secret,new_name,new_description,new_key_id);
            RETURN created_id;
          END;
          $$;
          SQL
      - name: Apply schema 133 and verify readiness
        run: |
          npm run db:migrate
          psql "$DATABASE_URL" -v ON_ERROR_STOP=1 -c "GRANT bls_platform_runtime TO buy_local_sparta"
          npm run db:ready
          psql "$DATABASE_URL" -v ON_ERROR_STOP=1 <<'SQL'
          SELECT to_regprocedure('bls_private.catalog_material_variant_conflict(jsonb,jsonb)') IS NOT NULL AS material_conflict_function_exists;
          SELECT to_regprocedure('bls_private.catalog_source_canonicalization_preview(text,uuid,numeric)') IS NOT NULL AS identity_preview_exists;
          SELECT to_regprocedure('bls_private.apply_catalog_source_canonicalization(text,uuid,uuid,uuid,numeric,integer)') IS NOT NULL AS identity_apply_exists;
          SELECT to_regprocedure('bls_private.match_submitted_vendor_product()') IS NOT NULL AS vendor_identity_matcher_exists;
          SQL
      - name: Exercise unified source catalogue identity policy
        run: psql "$DATABASE_URL" -v ON_ERROR_STOP=1 -f scripts/catalog-identity-unification-smoke.sql
      - name: Exercise vendor product identity-v2 policy
        run: psql "$DATABASE_URL" -v ON_ERROR_STOP=1 -f scripts/vendor-product-identity-v2-smoke.sql
