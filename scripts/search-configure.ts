import { createPostgresRuntimeFromEnv, EXPECTED_SCHEMA_VERSION } from "../packages/postgres-runtime/src/index.ts";
const runtime=createPostgresRuntimeFromEnv({applicationName:"buy-local-sparta-search-configure"});try{const db=await runtime.readiness(EXPECTED_SCHEMA_VERSION);if(!db.ok)throw new Error(db.message);if(!runtime.search)throw new Error("BLS_SEARCH_ENABLED=true and Meilisearch configuration are required");await runtime.search.configure();const health=await runtime.search.readiness();if(!health.ok)throw new Error(`Meilisearch health is ${health.status??"unknown"}`);console.log(JSON.stringify({ok:true,index:process.env.MEILISEARCH_INDEX_UID||"bls_products_v1",health}));}finally{await runtime.close();}
